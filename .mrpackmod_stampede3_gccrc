# https://github.com/DrTimothyAldenDavis/SuiteSparse/issues/1049
BLAS_VENDOR_FLAG = -D BLA_VENDOR=Intel10_64lp_seq

BLASLAPACK = mkl
BLASLAPACK_INC = ${TACC_MKL_INC}
BLASLAPACK_LIB = ${TACC_MKL_LIB}
# note the semicolons: be sure to single-quote this when using the macros
BLASLAPACK_LIBS = mkl_lapack_lp64_seq;mkl_lapack_core;mkl_blas_lp64_seq;mkl_blas_core;mkl_rt;pthread
BLASLAPACK_THREAD_LIBS = mkl_lapack_gf_lp64;mkl_lapack_core;mkl_blas_core;mkl_rt;pthread
# note the semicolons: be sure to single-quote this when using the macros
BLASLAPACK_LIBRARIES = ${TACC_MKL_LIB}/libmkl_lapack_lp64_seq.so;${TACC_MKL_LIB}/libmkl_lapack_core.so;${TACC_MKL_LIB}/libmkl_blas_lp64_seq.so;${TACC_MKL_LIB}/libmkl_blas_core.so

